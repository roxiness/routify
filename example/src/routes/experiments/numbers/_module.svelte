<script>
    export let payload
    let subRoutes = []
</script>

{#each subRoutes as subRoute}
    <div>
        <!-- <SubRoute route={params.slug === index ? activeRoute : someRoute({slug: index})} /> -->
    </div>
{/each}
