<h5>Replace</h5>
<p>
    This file <code>replace@custom/index.svelte</code> finds the nearest
    <code>_module-custom.svelte</code>. All modules between the root and the
    <code>custom</code>
    module are removed and the
    <code>custom</code> module is then added as the root of the render tree.
</p>
